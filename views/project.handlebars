<form class="form w-50 edit-form">
  <div class="form-group">
    <label for="project-name">project name:</label>
    <input id="name-edit" class="form-input" value="{{name}}" />
  </div>
  <div class="form-group">
    <label for="project-name">project description:</label>
    <textarea id="description-edit" class="form-input">{{description}}</textarea>
  </div>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button data-id="{{id}}" class="btn btn-primary">update post</button>
    <button class="btn btn-primary">delete post</button>
  </p>
</form>
<script src="/js/editPost.js"></script>